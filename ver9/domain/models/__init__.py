"""Canonical domain state models.

Immutable snapshots and value objects representing the state of the trading
system from the domain perspective. These are pure data containers with no
business logic or runtime dependencies.
"""
